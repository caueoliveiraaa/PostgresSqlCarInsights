# 📁 sql/ — Centralized SQL scripts, queries and logic

This folder contains all the SQL code used across the project. It stores all sorts of queries, for reading, updating, deleting and adding information into various database objects.

## 01 - create_scripts/

This subfolder stores all the SQL scripts releated creating database objects and inserting data.

✅ Ensures that all creation logic is stored in one place and is easy to get.</br>
✅ Numbers the files for better organization.</br>

🔧 When to use:

- When creating pre-defined database.</br>
- When creating pre-defined tables.</br>
- When creating new tables.</br>
- When inserting new rows in the database.</br>
- When inserting pre-defined rows in the database.</br>

## 02 - read_scripts/

This subfolder stores all the SQL scripts releated to getting data from database objects.

✅ Ensures that all reading logic is stored in one place and is easy to get.</br>
✅ Numbers the files for better organization.</br>

🔧 When to use:

- When reading pre-defined tables.</br>
- When reading new tables.</br>
- When reading new rows from tables.</br>
- When reading pre-defined rows from tables.</br>

## 03 - update_scripts/

This subfolder stores all the SQL scripts releated to updating database objects and raw data.

✅ Ensures that all updating logic is stored in one place and is easy to get.</br>
✅ Numbers the files for better organization.</br>

🔧 When to use:

- When updating tables in the database.</br>
- When updating rows from tables.</br>
- When updating columns from tables.</br>

## 04_delete_scripts/

This subfolder stores all the SQL scripts releated to dropping database objects and deleting data.

✅ Ensures that all deletion logic is stored in one place and is easy to get.</br>
✅ Numbers the files for better organization.</br>

🔧 When to use:

- When droppig pre-defined tables.</br>
- When dropping new tables.</br>
- When deleting new rows from tables.</br>
- When deleting pre-defined rows from tables.</br>

## 05_analysis_scripts/

This subfolder stores all the SQL scripts releated to analyzing data.</br>
It contains logic for calculating the average, sum or max of columns, for different types of joins on different tables, etc.

✅ Ensures that all analysis logic is stored in one place and is easy to get.</br>
✅ Numbers the files for better organization.</br>

🔧 When to use:

- When analyzing database objects in general.</br>
- When analyzing data inside the tables and other database objects.</br>

## How to Use This Folder

✅ Import method responsible for returning the content of the SQL files:

```python
from src.utils.sql_reader import get_content_of_sql_file
```

✅ Get SQL scripts via the recent imported method:

```python
sql_script: str = get_content_of_sql_file(
    file_name="01_database_drop", 
    folder_option=4
)
```

✅ Then execute the script with the proper database methods.

## When to Add New Files Here

Add a new file to **config/** when you need to store:

- New SQL queries for reading data.</br>
- New SQL queries for updating data.</br>
- New SQL queries for deleting data.</br>
- New SQL queries for inserting data.</br>
- New SQL queries for reading database objects.</br>
- New SQL queries for updating database objects.</br>
- New SQL queries for deleting database objects.</br>
- New SQL queries for inserting database objects.</br>
- New analysis logic for analysing data.</br>

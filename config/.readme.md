# 📁 config/ — Centralized Configuration Layer

This folder contains all configuration logic used across the project. It centralizes paths, environment variables, database settings, error mappings, and UI constants so the rest of the codebase stays clean, consistent, and maintainable.

## 1. Database Settings (**db_info.py**)

Loads and validates database environment variables using dotenv and a custom **get_env_value()** validator from **src/utils/**. It exposes constants such as:

![POSTGRES_USER](https://img.shields.io/badge/STR-POSTGRES__USER-blue)</br>
![POSTGRES_PASSWORD](https://img.shields.io/badge/STR-POSTGRES__PASSWORD-blue)</br>
![POSTGRES_HOST](https://img.shields.io/badge/STR-POSTGRES__HOST-blue)</br>
![POSTGRES_PORT](https://img.shields.io/badge/STR-POSTGRES__PORT-blue)</br>
***...***

✅ Ensures that required database credentials exist and are valid.</br>
✅ Prevents runtime errors caused by missing **.env** values.</br>
✅ Keeps sensitive data out of the codebase.</br>

🔧 When to use:

- Any module that needs to connect to PostgreSQL or any other database.</br>
- Database initialization scripts.</br>
- Other centralized database settings or credentials.</br>

## 2. Path Configuration (**paths.py**)

Stores all filesystem paths used across the project, such as:

![PATH_LOGS](https://img.shields.io/badge/STR-PATH__LOGS-blue)</br>
![PATH_SQL_SETUP](https://img.shields.io/badge/STR-PATH__SQL__SETUP-blue)</br>
***...***

✅ Ensures that every module uses the same paths, avoiding hard‑coded strings scattered throughout the codebase.

🔧 When to use:

- Anytime you need to load SQL files.</br>
- When writing logs.</br>
- When building new modules that need consistent directory access.</br>

## 3. psycopg2 Error Mapping (**pg_errors_map.py**)

Maps PostgreSQL error codes to human‑readable messages.</br>
Example categories include:

![DuplicateObject](https://img.shields.io/badge/42710-DUPLICATE_OBJECT-blue)</br>
![UniqueViolation](https://img.shields.io/badge/23505-UNIQUE_VIOLATION-blue)</br>
![UndefinedTable](https://img.shields.io/badge/42P01-UNDEFINED_TABLE-blue)</br>
![NotNullViolation](https://img.shields.io/badge/23502-NOT_NULL_VIOLATION-blue)</br>
![SerializationFailure](https://img.shields.io/badge/40001-SERIALIZATION_FAILURE-blue)</br>

✅ Provides clear error messages.</br>
✅ Handles database exceptions gracefully.</br>
✅ Improves debugging and logging.</br>

🔧 When to use:

- Database exception handling.</br>
- Logging systems.</br>
- User‑facing error messages.</br>

## 4. UI/CLI Styling Constants (**rich_info.py**)

Stores lists of colors, styles, programming languages, and themes used by **rich**, such as:

![RICH_COLORS](https://img.shields.io/badge/LIST[STR]-RICH__COLORS-blue)</br>
![RICH_STYLES](https://img.shields.io/badge/LIST[STR]-RICH__STYLES-blue)</br>
![RICH_LANGUAGES](https://img.shields.io/badge/LIST[STR]-RICH__LANGUAGES-blue)</br>

✅ Keeps UI styling consistent.</br>
✅ Makes it easy to add/remove themes.</br>
✅ Avoids magic strings in the UI code.</br>

🔧 When to use:

- CLI menus.</br>
- Syntax highlighting.</br>
- Theming.</br>
- Any feature using rich for output formatting.</br>

## 5. How to Use This Folder

✅ Import paths:

```python
from config.paths import PATH_SQL_QUERIES
```

✅ Import environment variables:

```python
from config.db_info import POSTGRES_USER
```

✅ Import error messages:

```python
from config.pg_errors_map import ERROR_MAP
```

✅ Import UI constants:

```python
from config.rich_info import RICH_COLORS
```

## 6. When to Add New Files Here

Add a new file to **config/** when you need to store:

- New global constants.</br>
- New environment variables.</br>
- New directory paths.</br>
- New error mappings.</br>
- New UI/CLI configuration.</br>
- Anything that multiple modules depend on.</br>
